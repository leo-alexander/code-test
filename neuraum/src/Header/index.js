import React from "react";
// import PropTypes from "prop-types";

import styles from "./header.css";

const Header = () => (
  <div className={styles.container}>This is the header</div>
)

export default Header
